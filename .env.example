# Ejemplo de archivo de variables de entorno
# Copia este archivo a .env.local y configura tus valores

# Base de datos PostgreSQL
# Para desarrollo local con Docker Compose:
DATABASE_URL=postgresql://escazu_user:escazu_password_2024@localhost:5432/herramienta_escazu

# Para producción con Neon:
# DATABASE_URL=postgresql://user:password@host.neon.tech/database?sslmode=require

# Otras variables de entorno (si se necesitan en el futuro)
# NEXT_PUBLIC_API_URL=http://localhost:3000
# JWT_SECRET=your-secret-key
